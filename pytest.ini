[pytest]
# asyncio_mode=auto: los tests `async def` corren sin decorador explícito.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = backend/tests
# Hace importable el paquete `backend` sin instalar (equivale a PYTHONPATH=.).
pythonpath = .
addopts = -q
