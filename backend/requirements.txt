# ===== FastAPI Core =====
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# ===== Database (async for FastAPI, sync for Celery) =====
sqlalchemy==2.0.36
alembic==1.14.0
greenlet==3.1.1
aiosqlite==0.20.0          # SQLite async (dev/local)
asyncpg==0.30.0            # PostgreSQL async (GCP Cloud SQL)
psycopg2-binary==2.9.10    # PostgreSQL sync (Alembic + Celery)

# ===== Celery + Redis =====
celery==5.4.0
redis==5.2.1

# ===== Settings =====
pydantic-settings==2.6.1
pydantic[email]==2.10.3

# ===== Security =====
python-jose[cryptography]==3.3.0
bcrypt==4.2.1              # Direct usage (passlib has bcrypt 4.x compatibility issues)
cryptography==43.0.3

# ===== Scheduling (session TTL cleanup) =====
APScheduler==3.10.4

# ===== HTTP =====
httpx==0.28.1

# ===== Core framework deps (shared with CLI) =====
jira==3.10.5
langchain-google-genai==4.2.2
google-generativeai==0.8.6
langchain==1.3.1
langchain-core==1.4.0
python-dotenv==1.0.1
